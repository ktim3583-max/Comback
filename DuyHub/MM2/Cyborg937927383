-- // DUYXD KEY SYSTEM - FIXED FULL VERSION

local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local UICorner_Main = Instance.new("UICorner")
local UIStroke_Main = Instance.new("UIStroke")
local Title_GetKey = Instance.new("TextLabel")
local KeyInput = Instance.new("TextBox")
local UICorner_Input = Instance.new("UICorner")
local UIStroke_Input = Instance.new("UIStroke")
local CheckBtn = Instance.new("TextButton")
local UICorner_Btn = Instance.new("UICorner")
local GetKeyBtn = Instance.new("TextButton")
local UICorner_Get = Instance.new("UICorner")
local SubTitle_Discord = Instance.new("TextLabel")

-- Anti duplicate
pcall(function()
    game.CoreGui.DuyXD_KeySystem_Fixed:Destroy()
end)

-- GUI
ScreenGui.Name = "DuyXD_KeySystem_Fixed"
ScreenGui.Parent = game.CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- Main
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(35,35,35)
MainFrame.Position = UDim2.new(0.5,-150,0.5,-100)
MainFrame.Size = UDim2.new(0,300,0,200)
MainFrame.Active = true
MainFrame.Draggable = true

UICorner_Main.CornerRadius = UDim.new(0,12)
UICorner_Main.Parent = MainFrame

UIStroke_Main.Color = Color3.fromRGB(255,0,0)
UIStroke_Main.Thickness = 2
UIStroke_Main.Parent = MainFrame

-- Title
Title_GetKey.Parent = MainFrame
Title_GetKey.BackgroundTransparency = 1
Title_GetKey.Position = UDim2.new(0,0,0,15)
Title_GetKey.Size = UDim2.new(1,0,0,30)
Title_GetKey.Font = Enum.Font.GothamBold
Title_GetKey.Text = "GET KEY"
Title_GetKey.TextColor3 = Color3.fromRGB(255,255,255)
Title_GetKey.TextSize = 22

-- Input
KeyInput.Parent = MainFrame
KeyInput.BackgroundColor3 = Color3.fromRGB(45,45,45)
KeyInput.Position = UDim2.new(0.05,0,0.35,0)
KeyInput.Size = UDim2.new(0.9,0,0,35)
KeyInput.Font = Enum.Font.Gotham
KeyInput.PlaceholderText = "Enter Key..."
KeyInput.Text = ""
KeyInput.TextColor3 = Color3.fromRGB(255,255,255)
KeyInput.TextSize = 14

UICorner_Input.CornerRadius = UDim.new(0,6)
UICorner_Input.Parent = KeyInput

UIStroke_Input.Color = Color3.fromRGB(255,0,0)
UIStroke_Input.Thickness = 1
UIStroke_Input.Parent = KeyInput

-- Check Button
CheckBtn.Parent = MainFrame
CheckBtn.BackgroundColor3 = Color3.fromRGB(255,0,0)
CheckBtn.Position = UDim2.new(0.25,0,0.6,0)
CheckBtn.Size = UDim2.new(0.5,0,0,35)
CheckBtn.Font = Enum.Font.GothamBold
CheckBtn.Text = "CHECK KEY"
CheckBtn.TextColor3 = Color3.fromRGB(255,255,255)
CheckBtn.TextSize = 14

UICorner_Btn.CornerRadius = UDim.new(0,6)
UICorner_Btn.Parent = CheckBtn

-- Get Key Button
GetKeyBtn.Parent = MainFrame
GetKeyBtn.BackgroundColor3 = Color3.fromRGB(60,60,60)
GetKeyBtn.Position = UDim2.new(0.25,0,0.82,0)
GetKeyBtn.Size = UDim2.new(0.5,0,0,25)
GetKeyBtn.Font = Enum.Font.GothamBold
GetKeyBtn.Text = "GET KEY LINK"
GetKeyBtn.TextColor3 = Color3.fromRGB(255,255,255)
GetKeyBtn.TextSize = 12

UICorner_Get.CornerRadius = UDim.new(0,6)
UICorner_Get.Parent = GetKeyBtn

-- Subtitle
SubTitle_Discord.Parent = MainFrame
SubTitle_Discord.BackgroundTransparency = 1
SubTitle_Discord.Position = UDim2.new(0,0,0.93,0)
SubTitle_Discord.Size = UDim2.new(1,0,0,15)
SubTitle_Discord.Font = Enum.Font.GothamItalic
SubTitle_Discord.Text = "Key System By DuyXD"
SubTitle_Discord.TextColor3 = Color3.fromRGB(150,150,150)
SubTitle_Discord.TextSize = 11

-- Key
local CorrectKey = "Vinysoua_key123"

-- Get Key
GetKeyBtn.MouseButton1Click:Connect(function()

    if setclipboard then
        setclipboard("https://link-target.net/YOUR_LINKVERTISE")
    end

    GetKeyBtn.Text = "COPIED!"

    task.wait(1)

    GetKeyBtn.Text = "GET KEY LINK"

end)

-- Check Key
CheckBtn.MouseButton1Click:Connect(function()

    local userInput = KeyInput.Text:match("^%s*(.-)%s*$")

    if userInput == CorrectKey then

        CheckBtn.Text = "LOADING..."
        CheckBtn.BackgroundColor3 = Color3.fromRGB(255,170,0)

        local success, err = pcall(function()

            loadstring(game:HttpGet(
                "https://raw.githubusercontent.com/ktim3583-max/Comback/main/DuyHub/MM2/Real"
            ))()

        end)

        if success then

            CheckBtn.Text = "SUCCESS!"
            CheckBtn.BackgroundColor3 = Color3.fromRGB(0,255,0)

            task.wait(1)

            ScreenGui:Destroy()

        else

            warn(err)

            CheckBtn.Text = "LOAD FAILED"
            CheckBtn.BackgroundColor3 = Color3.fromRGB(150,0,0)

            print(err)

            task.wait(2)

            CheckBtn.Text = "CHECK KEY"
            CheckBtn.BackgroundColor3 = Color3.fromRGB(255,0,0)

        end

    else

        CheckBtn.Text = "WRONG KEY!"
        CheckBtn.BackgroundColor3 = Color3.fromRGB(150,0,0)

        task.wait(1.5)

        CheckBtn.Text = "CHECK KEY"
        CheckBtn.BackgroundColor3 = Color3.fromRGB(255,0,0)

    end
end)
